import React from 'react';

export const [selectedCard, setSelectedCard] = React.useState(null);
export const [cards, setCards] = React.useState([]);
export const [isAddPlacePopupOpen, setIsAddPlacePopupOpen] = React.useState(false);
